Demo data for local manual testing of pvc-explorer-agent.

Suggested run command from the repository root:
  go run ./cmd/agent -root ./testdata/demo -pvc demo-pvc

This directory is intentionally small and safe to browse, edit, upload into,
and delete from while exercising the UI and API locally.
